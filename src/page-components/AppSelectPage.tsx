import React from 'react';
import cn from 'classnames';
import { AppSelect } from '../components/UI/AppSelect';

export const AppSelectPage = (): JSX.Element => {
  return (
    <div className={cn('select-page')}>
        <AppSelect
        
        />
    </div>
  )
}
